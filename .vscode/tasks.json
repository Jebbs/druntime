{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "${workspaceRoot}/build.sh",
            //"args": ["-j12", "-fposix.mak", "BUILD=debug"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}